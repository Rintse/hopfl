( ( λ rand_idx . λ flip . λ s . 
    flip rand_idx s
)
( ( λ draw . # rand_idx
    if (draw < 0.25) then 0.0
    else ( if (draw < 0.5) then 1.0
    else ( if (draw < 0.75) then 2.0
    else 3.0 ))
  ) (normal [0.5, 0.25])
)

( fix f . λ idx . λ s .
    if (idx = 0.0) then ([ !(fst s), snd s])
    else ([ fst s, f ⊙ (next (idx-1.0)) ⊙ (next (snd s)) ])
)
)

( [   
    true, [
    true, [
    true, [
    true, 0.0 
]]]] )
