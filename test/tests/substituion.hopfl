# Nested abstractions that use the same binding variable
# Tests for correct substition

( λ x . ( λ x . x ) x ) x

#( λ x . ( λ y . y ) x ) ( λ x . y )

#( λ x1 . ( λ x2 . x2 ) x1 ) ( λ x3 . x3 )

# ( λ x . ( λ y . x y ) ( λ x . y )
