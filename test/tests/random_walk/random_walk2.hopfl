# Takes the second number of a random walk
(next ( λ x . fst x ) ) ⊙ (
    (next (λ x . out x)) ⊙
    (snd out ( 
        (fix f . #:: ⊳ ( real × (μ X . real × ⊳ X) ) . 
            λ x . in ( < 
                x , 
                f ⊙ next( normal <x , s> )
            > )
        ) x0
    ) )
)
