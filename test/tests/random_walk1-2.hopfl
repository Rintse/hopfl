# Takes the first and second number of a random walk
( λ g . λ x . < fst x ,  g ⊙ (snd x) > )
(next ( λ x . fst ( out x )))
(out ( 
    (fix f . #:: ⊳ ( real × (μ X . real × ⊳ X) ) . 
        λ x . in ( < 
            x , 
            f ⊙ next( normal <x , s> )
        > )
    ) x0
) )
