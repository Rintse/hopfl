# Takes a the idx-th number out of a random walk
( fix f . λ idx . λ rw .
    if ( idx <= 0.0 ) then fst (out rw)
    else f ⊙ next (idx-1.0) ⊙ (snd (out rw))
) idx

# The random walk stream
( (fix f . 
    λ x . in ( [ x , f ⊙ next( normal [x , s] ) ] )
) x0 ) 

