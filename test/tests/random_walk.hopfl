# Takes the first number of a random walk

λ s . out ( 
    (fix f . #:: ⊳ ( real × (μ X . real × ⊳ X) ) . 
        λ x . in ( < 
            x , 
            f ⊙ next( normal <x , s> )
        > )
    ) x0
)
